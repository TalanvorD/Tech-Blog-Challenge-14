<div>
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.name}} on {{format_date createdAt}}
        </div>
        <div class="text">
            {{text}}
        </div>
    </section>
    {{/each}}
</div>